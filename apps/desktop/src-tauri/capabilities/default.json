{
  "$schema": "../../../../node_modules/@tauri-apps/cli/schemas/capabilities.schema.json",
  "identifier": "default",
  "description": "Capabilities for the main MARVIN desktop window. Intentionally narrow — everything the shell needs is already served by the embedded MARVIN web app on localhost:3030; the native side only exposes a TCP probe and shell.open for external-link handling.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open"
  ]
}
