{
  "name": "@marvin/desktop",
  "version": "1.0.0",
  "private": true,
  "description": "MARVIN as a native macOS app — Tauri wrapper around the localhost:3030 web shell.",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "prebuild": "../../scripts/fetch-node.sh aarch64-apple-darwin && ./scripts/bundle-resources.sh",
    "build": "tauri build",
    "build:debug": "tauri build --debug"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.4.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.1"
  }
}
