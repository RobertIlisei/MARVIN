node_modules/
.next/
.turbo/
dist/
*.tsbuildinfo

# MARVIN runtime data (transcripts, cost, cache) — not checked in
data/.marvin/
.env
.env.local

# graphify — check in the graph + report, ignore the extraction cache
# and the python-interpreter pointer.
graphify-out/cache/
graphify-out/.graphify_python

# Claude Code — local-only settings
.claude/settings.local.json

# Playwright MCP runtime cache (console logs, page dumps)
.playwright-mcp/

# OS / editor
.DS_Store
.vscode/
.idea/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
